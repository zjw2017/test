name: test
on:
  workflow_dispatch:
jobs:
  setup-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Write Github_ENV
        run: |
          echo "zjw=20030721" >> $GITHUB_ENV
      - name: Test Github_ENV
        run: |
          echo "$zjw" >> $GITHUB_ENV
      - name: Test Github_ENV Again
        run: |
          echo "${{ env.zjw }}" >> $GITHUB_ENV
