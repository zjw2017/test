name: test
on:
  workflow_dispatch:
jobs:
  setup-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Check Support FileSystems
        run: |
          cat /proc/filesystems
          uname -r