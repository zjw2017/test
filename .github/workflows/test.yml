name: test
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1.准备环境
        run: |
          echo "NEW_PACKAGE_NAME=123" >> "$GITHUB_ENV"
      - name: 2.准备环境
        run: |
          echo "$NEW_PACKAGE_NAME"
