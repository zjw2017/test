name: test
on:
  workflow_dispatch:
    inputs:
      device:
        description: "待操作的系统包下载地址"
        required: true
      build_device:
        description: "底包机型代号"
        required: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1.准备环境
        run: |
          echo "pack_version=free" >> "$GITHUB_ENV"
      - name: 2.测试环境变量
        run: |
          echo $pack_version
      - if: github.event.inputs.device == 'star' && env.pack_version == 'premium'
        name: 1.准备环境
        run: |
          find
      - if: github.event.inputs.device == 'star' && env.pack_version == 'free'
        name: 1.准备环境
        run: |
          find